spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:5432/${DB_NAME:customer_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD}

server:
  port: 8001

logging:
  level:
    root: WARN
    com.nexuswealth: INFO

